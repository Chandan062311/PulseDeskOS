<!-- Copy into .github/PULL_REQUEST_TEMPLATE.md on GitHub. Kept here so forks get it. -->
## What

## Principle check (code owns workflow / frozen contracts / open-closed / config-not-code / no secrets)

## Gates (`make test`, UI build, `claude plugin validate`, smoke)

## Evals (if gates/thresholds touched — goldens re-run?)
